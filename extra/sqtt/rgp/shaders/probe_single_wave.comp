#version 450
// 1-wave WG (32 threads) control: useful to compare wave→SIMD placement vs 2-wave.
layout(local_size_x = 32) in;
layout(set = 0, binding = 0) buffer B { float data[]; } buf;
void main() {
  uint i = gl_LocalInvocationID.x;
  buf.data[i] = buf.data[i] + 1.0;
}
