#version 450
// 4-wave WG (128 threads) control: wave→SIMD placement vs 1-wave/2-wave.
layout(local_size_x = 128) in;
layout(set = 0, binding = 0) buffer B { float data[]; } buf;
void main() {
  uint i = gl_LocalInvocationID.x;
  buf.data[i] = buf.data[i] + 1.0;
}
